{{>layout/header}}
<main id="explore">
	<ul class="explore__users u-default-box">
	{{#users}}
		<li class="explore__user">
			<div class="explore__content">
			<a href="/user/{{id}}">
				<img src="/images/avatar.jpg" />
				<div class="explore__info">
				<a />
					<span class="explore__username">{{username}}</span> 
					<span class="explore__full-name">{{name}}</span>
				</div>
			</div>
			{{^equalUserState}}
					{{#followState}}
						<button onclick="unFollow({{id}})" class="profile_edit_btn">팔로우취소</button>
					{{/followState}}
					{{^followState}}
						<button onclick="follow({{id}})" class="profile_follow_btn">팔로우</button>
					{{/followState}}
				{{/equalUserState}}
		</li>
		{{/users}}
	</ul>
</main>
<script src="/js/follow.js"></script>
{{>layout/footer}}
